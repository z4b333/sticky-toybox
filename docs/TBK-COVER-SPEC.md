# Where a book cover goes

**This document has been folded into [CONVERTER-SPEC.md](CONVERTER-SPEC.md),**
which covers the same ground and the rest of it: `.tbk` and `.epub` layout,
artwork inside an EPUB, the picture format, and the files the device writes
that a converter must leave alone.

Two documents describing one format drift apart, and the one nobody is reading
drifts first. Start there.

Quick pointers into it:

- covers, and which of the three routes wins — §6
- the `.tbi` picture format — §2
- the `.tbk` header and its embedded cover — §3
- artwork inside an EPUB — §5
